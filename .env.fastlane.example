# Fastlane Environment Variables Template
# Copy this file to .env.fastlane and fill in your values
# NEVER commit .env.fastlane to version control!

# ============================================================================
# ANDROID - Google Play Console
# ============================================================================

# JSON key file for Google Play Console API
# Generate this from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Select your project
# 3. Go to IAM & Admin > Service Accounts
# 4. Create service account with "Service Account User" role
# 5. Create JSON key
# 6. Store the full path here or set PLAY_STORE_JSON_KEY_DATA with the JSON content
PLAY_STORE_JSON_KEY=/path/to/your/google-play-api-key.json

# Or use base64 encoded JSON key content:
# PLAY_STORE_JSON_KEY_DATA={"type":"service_account","project_id":"..."}

# ============================================================================
# iOS - App Store Connect
# ============================================================================

# Apple ID for App Store Connect
APPLE_ID=mohamedazher@gmail.com

# App Store Connect Team ID (find in App Store Connect > Membership)
ITC_TEAM_ID=YOUR_APP_STORE_CONNECT_TEAM_ID

# Developer Portal Team ID (find in Apple Developer > Membership)
TEAM_ID=YOUR_DEVELOPER_PORTAL_TEAM_ID

# App Store Connect API Key (recommended over username/password)
# Generate from: App Store Connect > Users and Access > Keys
APP_STORE_CONNECT_API_KEY_ID=YOUR_KEY_ID
APP_STORE_CONNECT_API_ISSUER_ID=YOUR_ISSUER_ID
APP_STORE_CONNECT_API_KEY_CONTENT=-----BEGIN PRIVATE KEY-----
YOUR_PRIVATE_KEY_CONTENT
-----END PRIVATE KEY-----

# ============================================================================
# MATCH (iOS Code Signing)
# ============================================================================

# Git repo for storing certificates and profiles
MATCH_GIT_URL=git@github.com:your-org/certificates.git

# Passphrase to encrypt/decrypt certificates
MATCH_PASSWORD=your-secure-passphrase

# ============================================================================
# CI/CD (GitHub Actions)
# ============================================================================

# For GitHub Actions, add these as GitHub Secrets:
# - PLAY_STORE_JSON_KEY_DATA (base64 encoded JSON key)
# - APPLE_ID
# - APP_STORE_CONNECT_API_KEY_ID
# - APP_STORE_CONNECT_API_ISSUER_ID
# - APP_STORE_CONNECT_API_KEY_CONTENT (base64 encoded)
# - MATCH_PASSWORD
# - MATCH_GIT_URL
